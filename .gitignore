# Jekyll build output & caches (never commit these)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local Ruby dependencies (if you install Jekyll later)
Gemfile.lock
vendor/
.bundle/

# OS / editor cruft
.DS_Store
Thumbs.db

# Internal planning docket — not published
PUNCH.md

# Python KB tooling
.venv/
__pycache__/
*.pyc

# Design agent's working folder — mockups/refactors, not published
_design/

# Continuity docs — local only, kept out of the public repo
CLAUDE.md
RESUME.md
